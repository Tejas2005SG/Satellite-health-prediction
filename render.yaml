# ============================================================
# Render Deployment Configuration
# ============================================================
# Create this file in your GitHub repository root
# 
# To deploy:
# 1. Push code to GitHub
# 2. Go to render.com and create new "Web Service"
# 3. Connect your GitHub repository
# 4. Use these settings:
#    - Build Command: pip install -r requirementsDeploy.txt
#    - Start Command: uvicorn api_server:app --host 0.0.0.0 --port $PORT
# ============================================================

services:
  - type: web
    name: satellite-health-api
    env: python
    buildCommand: pip install -r requirementsDeploy.txt
    startCommand: uvicorn api_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: N2YO_API_KEY
        value: "QTNXRH-FDLX6C-959ZTS-5NT2"
    # Free tier: auto-sleeps after 15 min of inactivity
    # Cold start takes ~30-60 seconds
