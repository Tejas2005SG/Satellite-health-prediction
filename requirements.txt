# Satellite Health AI - Requirements
# Install with: pip install -r requirements.txt
# NOTE: tensorflow-cpu is used because Render.com servers are CPU-only (no GPU/CUDA)

# Core API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6

# ML/DL (CPU-only build for cloud deployment)
tensorflow-cpu>=2.15.0
numpy>=1.24.0

# Data & API
requests>=2.31.0
